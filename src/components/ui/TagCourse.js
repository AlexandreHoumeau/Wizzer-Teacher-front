import React from 'react'

const TagCourse = ({ title }) => {
  return (
    <div className='bg-primary-light px-4 py-2 rounded-4xl'>
      <p className='text-primary font-raleway font-medium'>{title}</p>
    </div>  )
}

export default TagCourse
